# Types

This page documents `PlatePrediction`, the main return type for inference, along with the shared type aliases
used across the library.

```python
from fast_plate_ocr import PlatePrediction
```

::: fast_plate_ocr.core.types
